<div class="bg-white shadow sm:rounded-lg border border-gray-200 mt-6">
  <div class="px-6 py-5 border-b border-gray-200 bg-gradient-to-r from-gray-50 to-gray-100">
     <h3 class="text-lg leading-6 font-medium text-gray-900"><%= t('components.team_member_metrics.title') %></h3>
     <p class="mt-1 max-w-2xl text-sm text-gray-500">
       <%= t('components.team_member_metrics.description') %>
     </p>
  </div>
  <div class="px-6 py-6 overflow-x-auto">
    <% if any_data? %>
      <table class="min-w-full divide-y divide-gray-200">
        <thead>
          <tr>
             <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider bg-gray-50"><%= t('components.team_member_metrics.metric') %></th>
            <% team_members.each do |member| %>
              <th class="px-4 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider bg-gray-50 min-w-[100px]">
                <div class="font-semibold text-gray-900"><%= member.full_name.split.first %></div>
              </th>
            <% end %>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200 bg-white">

          <!-- Competence Level Section -->
          <tr class="bg-indigo-50">
            <td colspan="<%= team_members.size + 1 %>" class="px-4 py-3 text-sm font-bold text-indigo-900 border-b border-indigo-200">
              <div class="flex items-center gap-2">
                 <span><%= t('components.team_member_metrics.competence_level') %></span>
                <span class="inline-flex items-center text-xs text-gray-600 bg-indigo-100/50 px-2 py-0.5 rounded">
                   <%= t('components.team_member_metrics.competence_level_hint') %>
                </span>
              </div>
            </td>
          </tr>
          <tr>
            <td class="px-4 py-2 text-sm font-medium text-gray-700"><%= t('components.team_member_metrics.total') %></td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:competence_level, :total) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:competence_level, :total_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="inline-flex items-center justify-center px-3 py-1 rounded-full text-sm font-bold bg-gray-100 text-gray-800">
                    <%= value %>
                  </span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-red-700 bg-red-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.high') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:competence_level, :high) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:competence_level, :high_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-yellow-700 bg-yellow-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.normal') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:competence_level, :normal) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:competence_level, :normal_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-green-700 bg-green-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.low') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:competence_level, :low) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:competence_level, :low_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>

          <!-- Universality Section -->
          <tr class="bg-blue-50">
            <td colspan="<%= team_members.size + 1 %>" class="px-4 py-3 text-sm font-bold text-blue-900 border-b border-blue-200">
              <div class="flex items-center gap-2">
                 <span><%= t('components.team_member_metrics.universality') %></span>
                <span class="inline-flex items-center text-xs text-gray-600 bg-blue-100/50 px-2 py-0.5 rounded">
                   <%= t('components.team_member_metrics.universality_hint') %>
                </span>
              </div>
            </td>
          </tr>
          <tr>
            <td class="px-4 py-2 text-sm font-medium text-gray-700"><%= t('components.team_member_metrics.total') %></td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:universality, :total) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:universality, :total_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="inline-flex items-center justify-center px-3 py-1 rounded-full text-sm font-bold bg-blue-100 text-blue-800">
                    <%= value %>
                  </span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-red-700 bg-red-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.high') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:universality, :high) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:universality, :high_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-yellow-700 bg-yellow-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.normal') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:universality, :normal) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:universality, :normal_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-green-700 bg-green-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.low') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:universality, :low) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:universality, :low_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change > 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change > 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>

          <!-- Expertise Concentration Section -->
          <tr class="bg-red-50">
            <td colspan="<%= team_members.size + 1 %>" class="px-4 py-3 text-sm font-bold text-red-900 border-b border-red-200">
              <div class="flex items-center gap-2">
                 <span><%= t('components.team_member_metrics.expertise_concentration') %></span>
                <span class="inline-flex items-center text-xs text-gray-600 bg-red-100/50 px-2 py-0.5 rounded">
                   <%= t('components.team_member_metrics.expertise_concentration_hint') %>
                </span>
              </div>
            </td>
          </tr>
          <tr>
            <td class="px-4 py-2 text-sm font-medium text-gray-700"><%= t('components.team_member_metrics.total') %></td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:expertise_concentration, :total) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:expertise_concentration, :total_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="inline-flex items-center justify-center px-3 py-1 rounded-full text-sm font-bold <%= value > 0 ? 'bg-red-100 text-red-800' : 'bg-green-100 text-green-800' %>">
                    <%= value %>
                  </span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change < 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change < 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-red-700 bg-red-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.high') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:expertise_concentration, :high) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:expertise_concentration, :high_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change < 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change < 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-yellow-700 bg-yellow-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.normal') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:expertise_concentration, :normal) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:expertise_concentration, :normal_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change < 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change < 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>
          <tr>
            <td class="px-4 py-2 pl-8">
              <span class="inline-flex items-center text-xs font-medium text-green-700 bg-green-50 px-2 py-0.5 rounded">
                <%= t('components.team_member_metrics.low') %>
              </span>
            </td>
            <% team_members.each do |member| %>
              <% value = team_member_metrics[member.id]&.dig(:expertise_concentration, :low) || 0 %>
              <% change = team_member_metrics[member.id]&.dig(:expertise_concentration, :low_change) %>
              <td class="px-4 py-2 text-center">
                <div class="relative inline-flex items-center justify-center">
                  <span class="text-sm"><%= value %></span>
                  <% if change && change != 0 %>
                    <span class="absolute -top-1 -right-6 text-[10px] font-medium flex items-center gap-0.5 <%= change < 0 ? 'text-green-600' : 'text-red-600' %>">
                      <% if change < 0 %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M3.293 9.707a1 1 0 010-1.414l6-6a1 1 0 011.414 0l6 6a1 1 0 01-1.414 1.414L11 5.414V17a1 1 0 11-2 0V5.414L4.707 9.707a1 1 0 01-1.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% else %>
                        <svg class="w-2 h-2" viewBox="0 0 20 20" fill="currentColor">
                          <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L9 14.586V3a1 1 0 012 0v11.586l4.293-4.293a1 1 0 011.414 0z" clip-rule="evenodd" />
                        </svg>
                      <% end %>
                      <%= change > 0 ? '+' : '' %><%= change %>
                    </span>
                  <% end %>
                </div>
              </td>
            <% end %>
          </tr>

        </tbody>
      </table>
    <% else %>
       <p class="text-sm text-gray-500 text-center py-4"><%= t('components.team_member_metrics.no_data') %></p>
    <% end %>
  </div>
</div>