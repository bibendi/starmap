<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title>Starmap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>

    <%= vite_stylesheet_tag "application", "data-turbo-track": "reload" %>
    <%= vite_javascript_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="<%= theme_class %> bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100" data-theme="<%= current_theme %>">
    <!-- Main navigation -->
     <nav class="nav">
       <div class="nav__container">
         <div class="nav__content">
           <div class="nav__section">
            <div class="flex-shrink-0 flex items-center space-x-4">
                <%= link_to root_path, class: "brand-link" do %>
                 Starmap
               <% end %>
              <% unless user_signed_in? %>
                   <%= link_to t('locale.sign_in'), new_user_session_path, class: "btn btn--primary" %>
              <% end %>
            </div>
             <div class="hidden sm:ml-6 sm:flex sm:space-x-4 items-center">
              <% if user_signed_in? %>
                  <% if policy(:navigation).show_unit? %>
                    <% if current_user.unit.present? %>
                      <%= link_to t('navigation.unit'), unit_path(current_user.unit), class: "nav-link #{current_page?(unit_path(current_user.unit)) ? 'nav-link--active' : 'nav-link--inactive'}" %>
                    <% else %>
                      <%= link_to t('navigation.unit'), units_path, class: "nav-link #{current_page?(units_path) ? 'nav-link--active' : 'nav-link--inactive'}" %>
                    <% end %>
                  <% end %>

                  <% if policy(:navigation).show_team? %>
                    <% if current_user.team %>
                      <%= link_to t('navigation.team'), team_path(current_user.team), class: "nav-link #{current_page?(team_path(current_user.team)) ? 'nav-link--active' : 'nav-link--inactive'}" %>
                    <% else %>
                      <%= link_to t('navigation.team'), teams_path, class: "nav-link #{current_page?(teams_path) ? 'nav-link--active' : 'nav-link--inactive'}" %>
                    <% end %>
                  <% end %>

                  <% if policy(:navigation).show_personal_dashboard? %>
                    <%= link_to user_path(current_user), class: "nav-link #{current_page?(user_path(current_user)) ? 'nav-link--active' : 'nav-link--inactive'} relative inline-flex" do %>
                      <%= t('navigation.profile') %>
                      <% if current_user.team_lead? || current_user.unit_lead? || current_user.admin? %>
                        <%= render "shared/nav_profile_badge", count: User.pending_approvals_count(current_user) %>
                      <% end %>
                    <% end %>
                  <% end %>

                <% if policy(:navigation).show_admin? %>
                    <%= link_to t('navigation.admin'), admin_quarters_path, class: "nav-link #{current_page?(admin_quarters_path) ? 'nav-link--active' : 'nav-link--inactive'}" %>
                <% end %>
              <% end %>
            </div>
          </div>
            <div class="nav__section space-x-4">
               <!-- Theme switcher -->
               <div class="relative group flex items-center overflow-visible" data-controller="theme">
                <button type="button"
                        data-action="click->theme#toggle:prevent"
                        data-theme-target="button"
                         class="theme-toggle"
                        aria-label="<%= t('theme.toggle') %>">
                  <svg data-theme-target="iconLight" class="w-5 h-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
                  </svg>
                  <svg data-theme-target="iconDark" class="w-5 h-5 hidden" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z" />
                  </svg>
                  <svg data-theme-target="iconSystem" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                </button>
                <div class="dropdown-menu" role="menu" tabindex="-1">
                   <button data-action="click->theme#setLight:prevent" class="dropdown-item" role="menuitem" tabindex="-1">
                    <%= t('theme.light') %>
                  </button>
                   <button data-action="click->theme#setDark:prevent" class="dropdown-item" role="menuitem" tabindex="-1">
                    <%= t('theme.dark') %>
                  </button>
                  <button data-action="click->theme#setSystem:prevent" class="dropdown-item" role="menuitem" tabindex="-1">
                     <%= t('theme.system') %>
                   </button>
                </div>
              </div>

              <!-- Language switcher -->
              <div class="relative group flex items-center overflow-visible">
                <button type="button" class="flex items-center space-x-2 text-sm text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 focus:outline-none">
                 <span><%= t("locale.names.#{I18n.locale}", default: I18n.locale.to_s) %></span>
                  <svg class="h-5 w-5 text-gray-400 dark:text-gray-500" viewBox="0 0 20 20" fill="currentColor">
                   <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                 </svg>
               </button>
                  <div class="dropdown-menu" role="menu" tabindex="-1">
                 <% I18n.available_locales.each do |locale| %>
                     <%= button_to t("locale.names.#{locale}", default: locale.to_s), switch_locale_path(locale: locale), method: :post, class: "dropdown-item", role: "menuitem", tabindex: "-1" %>
                 <% end %>
               </div>
             </div>

             <% if user_signed_in? %>
                 <span class="text-sm text-gray-700 dark:text-gray-300 inline-flex items-center"><%= current_user.display_name %></span>
                  <%= button_to t('locale.sign_out'), destroy_user_session_path, method: :delete, class: "btn btn--secondary", data: { turbo: false } %>
             <% end %>
           </div>
        </div>
      </div>
    </nav>

    <!-- Main container -->
     <div class="main-container mt-16">
      <!-- Flash messages -->
      <div id="flash">
        <%= render partial: "shared/flash" %>
      </div>

      <!-- Main content -->
      <main>
        <%= yield %>
      </main>
    </div>
  </body>
</html>
