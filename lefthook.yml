pre-commit:
  commands:
    rubocop:
      glob: "*.rb"
      run: bin/rubocop --force-exclusion {staged_files}
    biome:
      glob: "*.{js,jsx}"
      run: npx biome check --write --unsafe {staged_files}
      skip:
        - merge
        - rebase